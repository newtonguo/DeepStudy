server:
  port: 9000

spring:
  application:
    name: demo-consumer11
    version: 0.0.1
  mvc:
    throw-exception-if-no-handler-found: true
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true

com:
  deswaef:
    reddit:
      url: https://www.reddit.com/r


logging:
  file: logs/${spring.application.name}.log