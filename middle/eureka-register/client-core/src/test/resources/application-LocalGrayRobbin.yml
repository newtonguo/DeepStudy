server:
  port: 8761

spring:
  application:
    name: eureka
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false

ribbon:
  router:
    config:
      serverMark: GRAY
      candidateServerMarks: ONLINE1,*
